language: python

python:
    - 3.6

install:
    - pip install --upgrade cython

script:
    - pip install --upgrade scipy
    - pip install --upgrade astropy
    - pip install --upgrade nbodykit
    - pytest --runslow horizonground/tests/
